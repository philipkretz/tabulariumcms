<!-- Step 6: Complete -->
<div class="accordion-section bg-white rounded-2xl shadow-lg border-2 border-gray-200 mb-6" data-step="6">
    <div class="accordion-header bg-gray-200 text-gray-700 p-4 rounded-t-xl cursor-pointer flex justify-between items-center">
        <div class="flex items-center">
            <span class="text-2xl mr-3">6</span>
            <h2 class="text-xl font-bold">{{ 'setup.steps.complete'|trans }}</h2>
        </div>
        <i class="fas fa-chevron-down transition-transform"></i>
    </div>
    <div class="accordion-body p-6" style="display: none;">
        <div class="text-center mb-8">
            <div class="w-24 h-24 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
                <i class="fas fa-check text-green-600 text-5xl"></i>
            </div>
            <h3 class="text-2xl font-bold text-gray-800 mb-2">{{ 'setup.complete_title'|trans }}</h3>
            <p class="text-gray-600">{{ 'setup.complete_description'|trans }}</p>
        </div>

        <div class="bg-green-50 border border-green-200 rounded-lg p-6 mb-6">
            <h4 class="font-bold text-green-800 mb-4">{{ 'setup.complete_checklist'|trans }}</h4>
            <ul class="space-y-2">
                <li class="flex items-center text-green-700">
                    <i class="fas fa-check-circle mr-3"></i>
                    {{ 'setup.complete_requirements'|trans }}
                </li>
                <li class="flex items-center text-green-700">
                    <i class="fas fa-check-circle mr-3"></i>
                    {{ 'setup.complete_database'|trans }}
                </li>
                <li class="flex items-center text-green-700">
                    <i class="fas fa-check-circle mr-3"></i>
                    {{ 'setup.complete_migrations'|trans }}
                </li>
                <li class="flex items-center text-green-700">
                    <i class="fas fa-check-circle mr-3"></i>
                    {{ 'setup.complete_admin'|trans }}
                </li>
                <li class="flex items-center text-green-700">
                    <i class="fas fa-check-circle mr-3"></i>
                    {{ 'setup.complete_settings'|trans }}
                </li>
            </ul>
        </div>

        <button id="complete-setup-btn" onclick="completeSetup()"
                class="w-full bg-gradient-to-r from-green-600 to-green-700 hover:from-green-700 hover:to-green-800 text-white font-bold py-4 rounded-lg transition text-lg mb-6">
            <i class="fas fa-check mr-2"></i> {{ 'setup.complete_installation'|trans }}
        </button>

        <div id="setup-complete-actions" class="space-y-4" style="display: none;">
            <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                <h4 class="font-bold text-blue-800 mb-2">{{ 'setup.whats_next'|trans }}</h4>
                <p class="text-blue-700 text-sm">{{ 'setup.whats_next_description'|trans }}</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <a id="admin-url" href="{{ path('admin_login') }}"
                   class="flex items-center justify-center bg-gradient-to-r from-yellow-600 to-yellow-700 hover:from-yellow-700 hover:to-yellow-800 text-white font-bold py-4 px-6 rounded-lg transition">
                    <i class="fas fa-cog mr-2"></i> {{ 'setup.go_to_admin'|trans }}
                </a>
                <a href="{{ path('app_homepage') }}"
                   class="flex items-center justify-center bg-gradient-to-r from-gray-600 to-gray-700 hover:from-gray-700 hover:to-gray-800 text-white font-bold py-4 px-6 rounded-lg transition">
                    <i class="fas fa-home mr-2"></i> {{ 'setup.go_to_frontend'|trans }}
                </a>
            </div>
        </div>
    </div>
</div>
