{% extends 'base.html.twig' %}

{% block title %}Welcome{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <div class="text-center">
        <h1 class="text-4xl font-bold mb-4">{{ message }}</h1>
        <p class="text-lg mb-8">Get started by logging into the <a href="{{ path('admin_login') }}" class="text-blue-600 hover:underline">admin panel</a>.</p>
    </div>
</div>
{% endblock %}
